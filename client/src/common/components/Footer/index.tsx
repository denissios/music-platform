import React, {FC} from 'react';
import Player from "../Header/components/Player";

const Footer: FC = () => {
    return (
        <div>
            <Player/>
        </div>
    );
};

export default Footer;